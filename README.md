Objective:


To read cost reports exported from AWS, Azure, and GCP, normalize them under a standard scheme (Account, Provider, Service, Date, Cost, Region, Tags), and generate a consolidated dataset ready for dashboards or advanced analysis.
